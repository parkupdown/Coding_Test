



























SELECT MAX(SALARY), NAME FROM EMPLOYEES GROUP BY BRANCH_ID


SELECT BRANCH_ID, NAME FROM EMPLOYEES
WHERE SALARY IN (SELECT MAX(SALARY) FROM EMPLOYEES GROUP BY BRANCH_ID,SALARY)
ORDER BY BRANCH_ID,NAME

SELECT BRANCH_ID, NAME FROM EMPLOYEES


SELECT MAX(SALARY) FROM EMPLOYEES GROUP BY BRANCH_ID

SELECT * FROM EMPLOYEES
ORDER BY BRANCH_ID , SALARY DESC


SELECT NAME FROM EMPLOYEES

WHERE SALARY IN (SELECT MAX(SALARY) FROM EMPLOYEES GROUP BY BRANCH_ID)

SELECT * FROM EMPLOYEES

브랜드 별 최댓값을 가져오고

그 sarary가 최대값인 NAME을 가져온다.

SELECT BRANCH_ID,NAME FROM EMPLOYEES

GROUP BY BRANCH_ID


WHERE BRANCH_ID = SELECT BRANCH_ID FROM EMPLOYEES GROUP BY BRANCH_ID HAVING MAX(SALARY)
 AND SALARY = SELECT MAX(SALARY) FROM EMPLOYEES GROUP BY BRANCH_ID 

ORDER BY BRANCH_ID,NAME


SELECT BRANCH_ID,NAME FROM EMPLOYEES 

WHERE NAME = SELECT NAME FROM EMPLOYEES GROUP BY BRANCH_ID HAVING MAX(SALARY)

ORDER BY BRANCH_ID,NAME